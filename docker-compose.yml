services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: fwd_rag_demo
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: demo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
